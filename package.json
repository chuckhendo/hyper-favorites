{
  "name": "hyper-favorites",
  "keywords": [
    "hyper"
  ],
  "version": "0.1.6",
  "description": "Hyper plugin to add a configurable 'favorite commands' bar to the bottom of your terminal. Currently in an alpha state",
  "main": "compiled.js",
  "scripts": {
    "watch": "babel index.js --watch --out-file compiled.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Carl Henderson <chuckhendo@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react": "^6.24.1"
  },
  "dependencies": {
    "styled-components": "^2.1.2"
  }
}
